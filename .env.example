# Base URL of the wedding-manager-api backend
VITE_API_BASE_URL=http://localhost:5000

# Per treballar sense backend, no cal tocar res aquí: `npm run dev:mock` carrega .env.mock
# i serveix tota l'API des dels handlers de MSW a src/mocks/.
