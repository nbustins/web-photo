# Mode disseny: `npm run dev:mock` (o `bun run dev:mock`). No cal cap backend engegat.
# api.client.ts exigeix un baseUrl no buit encara que MSW intercepti abans del fetch.
VITE_API_BASE_URL=http://localhost:5000
VITE_ENABLE_MSW=true
